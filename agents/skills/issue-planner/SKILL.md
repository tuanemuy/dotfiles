---
name: issue-planner
description: "Issueの対応計画を2視点並列レビューのループで磨き込み、`.thread/{Issue番号}/plan.md` に出力するスキル。ユーザーが「Issue #123 の計画を立てて」「このIssueどう対応する？」「実装方針を考えて」「plan this issue」などと言ったとき、または Issue 番号・URL 付きで計画・方針・設計を求められたとき（曖昧な依頼でもまず計画すべきケースを含む）にトリガーする。実装まで求められている場合は issue-implement を使う。"
---

# Issue Planner

GitHub Issueに対する実装計画を、1人のプランナーが初稿を作成し、2視点並列レビュー → 修正のループ（周回数はその周の指摘の質で決まる）で磨き込むスキル。

## Workflow Overview

```
Issue取得 → spec-slice判定 → 規模判定（小規模 / 通常）
  → [spec-slice] ファストパス: spec とチェックリストから転記ベースで plan.md / steps.md 作成
    → レビューループなし → 完了
  → [小規模] 直接プランニング → plan.md / steps.md 作成 → 完了
  → [通常] 1プランナーで初稿作成 → plan.md / steps.md / research.md 作成
    → 設計判断があれば adr.md に記録
    → レビューループ（既定1周・問題点が出た周だけ継続・最大3周）:
       2視点並列レビュー（要件カバレッジ / アーキ・リスク）
       → 問題点を plan.md / steps.md に反映
       → 全レビュアーが「問題なし」なら終了、未解決なら再レビュー
    → 足場（research.md / plan-review/）を削除して完了
```

計画の成果物はライフサイクルで分ける（詳細は `references/plan-template.md`）:

| ファイル | 役割 | 寿命 |
| --- | --- | --- |
| `plan.md` | 契約 — 目的 / 受け入れ基準 / スコープ / リスク / テスト方針 | 全フェーズが読む |
| `steps.md` | 手順 — 設計 / 実装ステップ | 実装フェーズまで |
| `research.md` | 足場 — 調査結果 | 凍結時に削除 |
| `plan-review/` | 足場 — レビューファイル・台帳・履歴 | 凍結時に削除 |
| `adr.md` | 設計判断の唯一の記録先 | 恒久 |

## Step 1: Issue の取得と分析

Issue番号またはURLを受け取ったら、まず内容を把握する。

```bash
git fetch origin
gh issue view <Issue番号> --json title,body,labels,assignees,milestone,comments
```

調査対象のコードが古いままだと、既に解決済みの問題を前提に計画してしまう。`git fetch origin` でリモートを取り込んだうえで、ローカルの作業ツリーがベースブランチ（通常 `origin/main`）から大きく遅れていないかを `git status -sb` で確認する。遅れている場合はユーザーに取り込みを確認してから進める。

Issueが取得できない場合（番号違い、権限不足、リポジトリ違いなど）はユーザーに確認して正しい情報を得る。推測で進めない。

Issueの内容を読み、以下を整理する:
- **目的**: このIssueは何を達成したいのか
- **要件**: 具体的に何が求められているか
- **制約**: 明示的・暗黙的な制約はあるか
- **スコープ**: Issueの範囲に含まれるもの・含まれないもの
- **コメントの要点**: コメント欄での議論・方針変更・合意事項があれば要約する（コメントには最新の合意や却下されたアプローチなど重要な情報が含まれることが多い）

### 既存計画の確認

`.thread/{Issue番号}/plan.md` が既に存在するかチェックする。存在する場合はユーザーに以下を確認する:
- 既存の計画を上書きして新しく作り直すか
- 既存の計画をベースに更新するか

ユーザーの指示に従って進める。

### spec-slice Issue のファストパス

Issue 本文の先頭に `<!-- spec-slice -->` マーカーがある場合（spec-to-issues が起票した縦スライス Issue）、フルの計画フローを通さない。spec は Issue をまたぐ決定（用語・境界・ビジネスルール・ポート契約）がレビュー済みで、Issue 本文にはチェックリスト（`spec/inventory/` 由来）・依存・検証方法が自己完結で書かれている — 計画はこの2つからの**転記**であって再導出ではない。spec が持たない実装詳細（DTO のフィールド・エラー分岐・境界値）は steps.md で先取りせず、実装フェーズがコードを読んで決める。

サブエージェント1体（**審査区分**）に plan.md / steps.md を作らせる:

- **plan.md** — 目的は Issue 本文の「目的」から。受け入れ基準の表にはチェックリスト全行を転記せず、「実装チェックリスト全行の実装（Issue #{番号} 本文参照。スタブ・仮実装不可）」を1行の基準とし、対象シナリオが end-to-end で動くことをシナリオレベルの基準として添える。スコープは対象シナリオと依存 Issue から、テスト方針はチェックリストの TC 行から書く。**規模メタデータは「通常」と記録する**（ファストパスで省略するのは計画レビューだけで、Phase 3 の実装レビューはフルで行う）
- **steps.md** — 「設計」セクションは新規設計せず、チェックリスト行の定義場所（`spec/domains/` 等）への参照で埋める。「実装ステップ」はチェックリスト行をレイヤーの依存順（domain → adapter → usecase → frontend → test）に並べ替えて束ねたもの。各ステップに対応する台帳 ID と spec の参照先を書く

research.md は作らない。Step 3（ADR）は転記中に設計判断が生じた場合のみ。**Step 4（レビューループ）はスキップする** — 計画の妥当性は spec のレビューループで担保済みで、転記に再レビューを重ねない。転記元の spec と Issue 本文が矛盾している場合だけは機械的に進めず、ユーザーに確認する。

受け入れ基準の観測方法は Issue 本文の「検証」から書く。Step 5 の完了報告にはレビューループの行の代わりに「spec-slice ファストパス（計画レビュー省略）」と記す。

### 規模の判定

判定するのは「計画づくりに調査とレビューループを回すか」だけ。**2択で、既定は「通常」。**

- **小規模** — 変更対象が1-2ファイル程度に閉じ、設計判断が要らない（typo修正、設定値変更、小さなバグ修正など）。サブエージェントの委譲は行わず、自分で直接コードを調査して `references/plan-template.md` に沿って plan.md と steps.md を出力する（research.md は作らない）。Step 3（ADR）は不要、Step 4（レビューループ）もスキップし、Step 5（凍結・完了報告）へ進む
- **通常** — 小規模と言い切れないものすべて。Step 2 に進み、1プランナーで初稿を作成する

plan.md には `**規模:** 小規模` / `**規模:** 通常` のどちらかを記録する。**この判定はレビューの周回数にも観点数にも効かない** — 周回数は Step 4 でレビュー結果から、実装レビューの観点数は実際の差分から決まる。だから迷ったら通常にしてよく、そのぶんコストが跳ねることもない。

## Step 2: 実装計画の作成

サブエージェントを1つ起動して初稿を作成する（**審査区分**。委譲方式とモデル選択は `../_shared/references/subagent-policy.md` に従う）。後続のレビューループで品質を担保するため、プランナーには特定の視点を強く与えず、Issueの要件を満たすバランスの取れた計画を立てさせる。

トークン効率の原則: プランナーは各ファイルを**直接書き込み**、メインには短いサマリーだけを返す。計画全文をメインのコンテキストに往復させない。テンプレートは本スキルの `references/plan-template.md` にあり、メインはプロンプトにその**絶対パス**を埋めて渡す（テンプレート本文は転記しない）。

### エージェントへのプロンプト

```
あなたはIssueの実装計画を立てるエンジニアです。
Issueの要件を満たし、**プロジェクトとしてあるべきアーキテクチャ**に沿った実装計画を立ててください。

## 対象Issue
- Issue番号: #{Issue番号}
- タイトル: {タイトル}
- 内容:
{Issueの本文}
- コメント・議論の要点:
{関連するコメントの要約 — 方針変更や合意事項があれば必ず含める}

## やること
1. `gh issue view {Issue番号}` でIssueの詳細とコメントを確認する
2. **プロジェクトのあるべきアーキテクチャを把握する** — `spec/` 配下の設計ドキュメント、`CLAUDE.md`、`README.md`、アーキテクチャ関連ドキュメントを読み、プロジェクトが目指している設計・レイヤー構造・規約を理解する
3. Issueに関連する既存のコードを調査する（検索ツールやファイル参照ツールを使って徹底的に）
4. 既存コードが「あるべき姿」と一致しているか、それとも乖離しているかを判断する
5. 調査でわかったことを `.thread/{Issue番号}/research.md` に書く（ディレクトリがなければ作成）
6. 実装計画を立て、{plan-template.md の絶対パス} のテンプレートに従って**直接書き込む**（規模メタデータは「{規模}」と記載）:
   - `.thread/{Issue番号}/plan.md` — 契約
   - `.thread/{Issue番号}/steps.md` — 手順
   テンプレートは下限であって上限ではない — 必須項目を満たした上で、必要なセクション・分析は自由に足してよい
7. 技術的な設計判断（アーキテクチャ・データモデル・ライブラリ選定など、トレードオフのある選択）があれば、同テンプレートの形式で `.thread/{Issue番号}/adr.md` にも書く。なければ作らない。**設計判断の記録先は adr.md だけ** — plan.md / steps.md に要約を重複させない

## 重要な原則
- **Issueの要件を満たすことに集中する** — スコープ外の改善やリファクタリングは含めない
- **設計はレイヤーの内側から考える** — ファイル変更の列挙から入らない。バックエンドに触れる変更ならまずドメインモデルへの影響（エンティティ・値オブジェクト・不変条件・ポート）から設計し、ユースケース → アダプター → UI の順に外側へ広げ、実装ステップはその設計から導出する
- **spec が書いていない詳細は、この計画で決める** — `spec/` が持つのは Issue をまたぐ決定（用語・ドメインと境界・ビジネスルール・ポート契約・画面構成）だけ。DTO の具体的なフィールド、エラー分岐、境界値、処理手順は spec に無くて当然で、コードを読んで決めるのがこの計画の仕事。「spec に書かれていないから設計漏れ」とは扱わない。ただし決めた結果が spec の持ち分（境界・用語・ルール・契約）を変えるなら、それは spec-revise の領分なので adr.md に記録して報告する
- **既存コードを盲目的に模倣しない** — 既存コードはあくまで現状の参考。プロジェクトのあるべきアーキテクチャ・設計ドキュメントが正であり、既存実装がそれと乖離している場合は、Issue範囲内で理想形に寄せる
- 既存コードと理想形が一致している箇所では、確立されたパターンを尊重する
- 変更の影響範囲を明確にする

## 返答（計画全文は返さない）
ファイルへの書き込みが済んだら、返答には**サマリーだけ**を返す:
- 実装方針の要約（1-2文）
- 実装ステップ数
- 設計判断の有無（adr.md を作成したか）
- 主要なリスク（一行ずつ）
- 判断に迷った点（あれば）
```

エージェントが失敗・タイムアウトした場合は、自分で同じ手順をなぞって計画ファイルを作成する。

## Step 3: ADR の確認

ADR はプランナーが Step 2 で必要に応じて作成済み。メインはサマリーで有無を把握するだけでよい（内容の転記・整形はしない）。レビューループ中に新たな設計判断が生じた場合は、反映を担当するサブエージェントが adr.md に追記する。

## Step 4: レビューループ

計画ファイルが揃ったら、**2つのサブエージェントを並列に起動してレビュー → 反映** を繰り返す（レビューは**審査区分**、反映は**判断区分**。委譲方式とモデル選択は `../_shared/references/subagent-policy.md` に従う）。各エージェントには異なる視点を与えることで、同一プロンプトで収束する問題を回避する。

レビューの観点は「要件カバレッジ」と「アーキテクチャ整合性・リスク」の2つに絞る — 計画段階で最も致命的になりやすい2点を集中的に検証する。Phase 3（実装後の PR レビュー）でレイヤー別の多角レビューが入る前提で、ここは要件と実現可能性の確認に振り切る。

### 周回数

**既定は1周、上限3周。** 計画はコードが1行もない段階の成果物で、詰め切れなかった点は実装フェーズが plan.md の「未解決事項」から拾える — 周回を重ねるより早く実装に渡すほうが安い。次の周に進むのは、その周に**問題点（P-）を取り込んだとき**だけ。

### 終了条件

以下のいずれか先に到達したら終了:

- **その周で「取り込む」と仕分けた指摘がゼロ** — 全レビュアーが「問題点ゼロ」を報告した場合に加え、残った指摘がすべて見送り判定（台帳に記録済み）の場合も終了とする
- **その周に取り込んだのが改善提案（S-）だけ** — 反映まで済ませて終了する。S- はフレッシュなレビュアーが毎周いくらでも出せるので、周回を続ける根拠にならない
- **上限の周回数（3周）に到達** — 残課題があれば plan.md 末尾の「未解決事項」セクションに明記して終了し、実装フェーズに引き継ぐ

### 各周の流れ

1. 2エージェント並列でレビュー実施。各レビュアーはレビュー全文を `.thread/{Issue番号}/plan-review/round-{N}-{視点キー}.md` に**直接書き**、メインには件数と指摘の一行リストだけを返す（視点キー: `coverage` / `arch-risk`）
2. メインは一行リストを見て対応方針を決める（同じ問題の指摘は1件として扱う。改善提案はスコープ内で妥当なもののみ取り込む）。仕分けの判定は `_shared/references/review-loop.md` の指摘台帳方式で `.thread/{Issue番号}/plan-review/triage.md` に記録する（「取り込む」= `fix`、「見送る」= `wont-fix`）。Key が一致する既出指摘は判定を継承して再審議しない
3. 問題点があれば、**反映用サブエージェント（判断区分）を1つ起動**して修正させる。渡すもの: 該当周のレビューファイルのパス（2つ）、plan.md / steps.md / adr.md のパス、メインが決めた対応方針（取り込む/見送る指摘のIDと理由）。サブエージェントは該当ファイルを修正し、設計判断があれば adr.md に追記し、`.thread/{Issue番号}/plan-review/history.md` に反映内容を追記して（書式は `references/plan-template.md`）、変更点の一行サマリーだけ返す
4. 終了条件に当てはまれば終了（history.md に「{N}周目: {終了理由}で終了」と追記）、取り込んだ問題点（P-）があれば次の周に進む

いずれかのレビュアーが失敗・タイムアウトした場合は、成功した結果だけで進める。両方失敗した場合はその周をスキップして次の周に進む（連続2周失敗ならレビューループを打ち切り、未解決事項として記録）。

### 共通テンプレート

各エージェントには以下のテンプレートを渡す（**視点** の部分だけ差し替える）:

```
あなたはIssueの実装計画をレビューするエンジニアです。
あなたの視点: **{視点}**

## レビュー対象（.thread/{Issue番号}/ 配下）
- `plan.md` — 契約
- `steps.md` — 手順
- `research.md` — プランナーの調査結果（存在する場合）
- `adr.md` — 設計判断（存在する場合）

## やること
1. `gh issue view {Issue番号}` でIssueの要件とコメントでの議論を確認する
2. `plan.md` と `steps.md` を読む
3. `research.md` / `adr.md` が存在すれば読み、調査の妥当性・設計判断の妥当性も確認する
4. 計画に関連する既存コードを調査する
5. 上記の視点で計画をレビューし、結果を `.thread/{Issue番号}/plan-review/round-{周番号}-{視点キー}.md` に**直接書き込む**

## 重要な原則
- **Issueの要件を満たすかどうかが最重要** — スコープ外の提案はしない
- Issueのコメント欄での議論・合意事項と計画が矛盾していないか確認する
- 具体的で実行可能なフィードバックを出す
- 問題点が見当たらない場合は「問題点ゼロ」と明示する（無理に粗探しをしない）

## レビューファイルのフォーマット

#### 問題点（要修正）
- **[P-001]** {問題の説明}
  - 場所: {plan.md / steps.md 等のファイルとセクション}
  - 理由: {なぜ問題か}
  - 提案: {どう修正すべきか}

（問題点がなければ「問題点ゼロ」と明記）

#### 改善提案（検討推奨）
- **[S-001]** {提案の説明}
  - 場所: {対象のファイルとセクション}
  - 理由: {なぜ改善になるか}

このフォーマットは下限であって上限ではない。必須なのは ID 付きの指摘（P-/S-、理由・提案）だけ。
分析の過程・前提・調査で得た知見など、伝えるべきことがあれば自由にセクションを足して深く書いてよい。

## 返答（レビュー全文は返さない）
ファイルへの書き込みが済んだら、返答には**サマリーだけ**を返す:
- 問題点: {数} / 改善提案: {数}
- 各指摘の一行リスト: `[P-001] {場所} — {タイトル}` / `[S-001] {場所} — {タイトル}`

**この2項目だけ。** 分析の経緯・引用・提案の再掲は返さない（詳細はレビューファイルにある）。
```

### 視点1: 要件カバレッジ・スコープ整合性

```
**Issueの要件カバレッジ・スコープ整合性** を重視してレビューしてください。plan.md の「受け入れ基準」表を起点に、(1) Issue本文・コメントで合意された要件がすべて基準に落ちているか（漏れた要件はないか）、(2) 各基準が検証可能な形で書かれ、観測方法（手段と何を見るか）が具体か — 手順・セレクタ・行番号・出力文字列が紛れていれば指摘する、(3) 基準と steps.md の実装ステップの紐づけが正しいか、(4) スコープ外の作業が紛れ込んでいないか、を徹底的に確認してください。
```

### 視点2: アーキテクチャ整合性・リスク

```
**プロジェクトのあるべきアーキテクチャとの整合性・実現可能性・リスク** を重視してレビューしてください。`spec/` 配下の設計ドキュメント・`CLAUDE.md`・アーキテクチャ関連ドキュメントを読み、プロジェクトが目指している設計・レイヤー構造・規約に計画が沿っているかを確認してください。research.md があれば、プランナーのコード理解そのものが正しいかも検証してください。steps.md の「設計」セクションがレイヤーの内側（ドメインモデル）から考えられているか — ドメインに置くべきロジックがユースケースやアダプターに漏れていないか、実装ステップが依存方向の順になっているか — も確認してください。既存コードに合わせるだけで「あるべき姿」から外れていないか、逆にIssue範囲を超えて理想形を追求しすぎていないかを両方向で見てください。加えて、提案された手順が現実的に動くか、見落とされた依存関係や副作用はないか、エッジケースは考慮されているか、設計判断のトレードオフは適切かを徹底的に確認してください。

なお `spec/` は Issue をまたぐ決定（用語・ドメインと境界・ビジネスルール・ポート契約・画面構成）だけを持ち、DTO のフィールド・エラー分岐・境界値・処理手順は意図的に書いていません。それらを計画が決めていることは正常で、「spec に定義がない」を問題点にはしないでください。問題にすべきなのは、計画が spec の**持ち分に矛盾している**場合と、**持ち分を暗黙に変更している**（spec-revise が必要なのに adr.md にも記録がない）場合です。
```

### レビュー履歴・未解決事項

各周の反映内容は `plan-review/history.md` に、上限の周回数に達しても残った問題は plan.md 末尾の「未解決事項」セクションに記録する。書式は `references/plan-template.md` を正とし、追記は反映用サブエージェントが行う（上限到達時の未解決事項はメインが反映用サブエージェントに指示して書かせる）。

恒久的に残す価値があるもの（設計判断 → adr.md、未解決の問題 → 未解決事項）だけを転記し、残りは plan-review/ ごと凍結時に捨てる。

## Step 5: 計画の凍結・完了報告

完了報告の前に足場ファイルを削除する。残したままにすると後続セッションの検索に紛れ込んでコンテキストを浪費する。

```bash
rm -rf .thread/{Issue番号}/plan-review/ .thread/{Issue番号}/research.md
```

削除の前に、恒久的に残す価値があるものを転記済みか確認する:

- **設計判断** → adr.md に書かれているか
- **上限到達で残った問題** → plan.md 末尾の「未解決事項」に書かれているか
- **research.md の内容** → 転記不要。設計は steps.md、影響範囲・注意点は plan.md のリスクに消費済みで、後続フェーズは `spec/`・`CLAUDE.md`・コード本体を直接読む

**成果物（plan.md / steps.md / adr.md）は残す。**

計画が完成したら、サマリーを出す。小規模Issueの場合は該当しない項目を省略する。

```
Issue #{Issue番号} の実装計画が完成しました！

- 契約: .thread/{Issue番号}/plan.md
- 実装手順: .thread/{Issue番号}/steps.md（ステップ数: {数}）
- 設計判断: .thread/{Issue番号}/adr.md（{あり/なし}）
- レビューループ: {N}周（終了理由: {取り込む指摘ゼロ / 改善提案のみ / 上限3周に到達}）
- 反映した修正: {数}件 / 取り込んだ改善提案: {数}件
- 未解決事項: {数}件（あれば plan.md 末尾を参照）
- 足場: 削除済み（plan-review/ / research.md）
```
