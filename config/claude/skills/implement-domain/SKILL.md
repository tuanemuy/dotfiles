---
name: implement-domain
description: >
  spec/domains/ の設計に基づいてドメインモデルを実装し、レビューサイクルで品質を担保するスキル。
  サブエージェントに実装を委譲し、メインエージェントはオーケストレーションに集中する。
  実装完了後にドメイン層に特化したコードレビューを行い、設計との整合性を確認する。
  ユーザーが「ドメイン実装して」「ドメインモデル作って」「エンティティ実装して」
  「implement domains」「ドメイン層やって」などと言ったときにトリガーする。
  spec/domains/ が存在する状態でドメイン層の実装を依頼された場合にこのスキルを使うこと。
  全レイヤーの実装を依頼された場合は implement スキルを使うこと。本スキルはドメイン層単体の実装に特化している。
---

# Implement Domain — ドメインモデル実装＋レビュー

spec/domains/ の設計に基づき、ドメインモデルをサブエージェントで実装する。実装後にドメイン層に特化したレビューサイクルを回し、設計との整合性を担保する。

**メインエージェントの役割はオーケストレーションのみ。** コードの読み書きはすべてサブエージェントに委譲する。

## Phase 1: 準備

### spec/ の確認

以下が存在するか確認する:

- `spec/domains/` — ドメイン設計

存在しない場合はユーザーに確認する。

### プロジェクトコンテキストの把握

CLAUDE.md と README.md を読み、以下を確認する:

- 使用言語・フレームワーク
- ディレクトリ構成（ドメイン層の配置先）
- typecheck・lint コマンド
- コーディング規約

### 進捗管理

`spec/progress.md` を確認し、ドメイン層の状態を把握する。なければ新規作成する。

### 実装例の確認

`docs/backend_implementation_example.md` が存在するか確認する。存在する場合はサブエージェントに参照先として渡す。

## Phase 2: 実装

`references/impl-guide.md` を読み、その手順に従ってドメインモデルを実装する。

ドメインごとにサブエージェントを起動する。独立したドメインは並列で起動してよい。ドメイン間に依存関係がある場合は、依存先を先に実装してから依存元を起動する（spec/domains/ の設計から依存関係を読み取る）。

各サブエージェント完了後:

1. 報告を確認する
2. typecheck + lint を実行する
3. エラーがあればサブエージェントを再起動して修正させる
4. `spec/progress.md` を更新する

## Phase 3: レビューサイクル

全ドメインの実装が完了したら、`references/review-guide.md` を読み、その手順に従ってレビューと修正を行う。

**2回連続で Blocker 0件 かつ Warning 0件** になったらレビュー完了。最大5ラウンド。5ラウンドで収束しない場合はユーザーに報告して判断を委ねる。

レビュー記録は `.review/domain/review-{連番}.md` に保存する。

## Phase 4: 完了

1. サブエージェントから報告された設計との乖離を確認し、必要に応じて:
   - `spec/domains/` の該当ドキュメントを更新する
   - 設計判断があった場合は `.adr/{連番}-{topic}.md` に記録する
2. git commit する
3. `spec/progress.md` を更新する
4. サマリーを出す

## 原則

- **メインエージェントはオーケストレーションのみ** — コードの読み書きはサブエージェントに委譲する
- **プロジェクトコンテキストに従う** — 言語・ディレクトリ構成・規約は CLAUDE.md / README.md から読み取る
- 設計ドキュメントに忠実に実装する — スコープ外の改善を含めない
- **並列実行時はドメイン間の依存関係を尊重する**
- **TODO・FIXME・仮実装を残さない**
- 品質チェック（typecheck + lint）はごまかさない
- **spec ドキュメントを実装と同期させる** — 設計の誤りや不足を発見したら spec を更新する
- **設計判断は `.adr/` に記録する** — 設計を変更・拡張した理由を ADR として残す
- **サブエージェントには必ずプロジェクトルートの CLAUDE.md を最初に読むよう指示する**
